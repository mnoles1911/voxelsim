THE CORDUROY IS MANUFACTURED IN B3: ONE GLOBAL REPOSE ANGLE PLANES THE FACES
============================================================================
2026-07-30. Thirteenth attempt at the parallel contour-terrace artifact, and the
first to locate WHERE the smoothness is created rather than proving one more
place it cannot be removed.

WHERE THE PREVIOUS FRAMING WAS RIGHT, AND WHERE IT STOPPED SHORT. The
contour-crookedness file proves the client cannot fix this (the carrier alone
measures D = 1.006; the drainage cap forbids exactly the gradients that would
crook a contour; every rotated-frame construction degenerates at 69 km), and its
closing geometric argument -- closed contours require the pit condition -- is
correct as stated. But it proves the wrong thing dispositive: real steep-slope
contours are NOT closed either. Open, parallel-ish contours look natural when
their SPACING varies, because spacing is 100 mm / local grade, and real ground
varies its grade at every scale. So the question is not "how do we close or
crook contours against the cap", it is "why is the local grade of our steep
faces so nearly CONSTANT". That question has a measurable answer, and it is not
"because a heightfield must be smooth".

THE MEASUREMENT. Stage-differenced bake of the g35 exemplar (tile -5,2, seed
20260719, bake_ver 4, no superblock -- statistics only), slope distribution of
the 1.875 m surface over the central 3.84 km window (repro site):

                          p10     p50     p90    frac within +-7% of tan(36)
  B0+B1 carrier+rough    0.263   0.480   0.674        7.7%
  B2d incised            0.328   0.626   1.246       10.7%
  B3 relaxed             0.344   0.706   0.903       19.1%

  B3 moved 40% of cells by >0.1 m (rms 1.33 m, p99 5.3 m), and the histogram
  says exactly what it did: the entire >0.85 tail incision carved is collapsed
  into a spike at 0.70-0.85, i.e. at tan(36 deg) = 0.727. The density JUMPS
  discontinuously at the repose value (4.8% in the 0.65-0.70 bin, 9.1% in
  0.70-0.75, 14.1% in 0.75-0.80). A third of a mountainside within +-10% of one
  angle is a constant-grade surface, and a constant-grade surface voxelises to
  evenly spaced parallel terraces. The corduroy is not an emergent property of
  heightfields; it is the fixed point of a relaxation operator with one global
  threshold.

  (Note B2d's distribution -- broad, heavy-tailed -- is the shape a real steep
  DTM has. The bake CARVES believable variance and then erases it. "Carved then
  erased" is directly visible in the stage diff, which is what stage sinks were
  built for.)

WHY THIS ESCAPES THE GEOMETRIC ARGUMENT. Nothing here perturbs a ramp against
the drainage cap. The fix changes the angle each face is driven TOWARD, in the
bake, before the fill/probe contract is measured. The client is untouched: no
worldgen change, no HLSL mirror, no version bump, no float-ban exposure.
kWorldGenVersion stays where it is; provider_id rolls through bake_ver 5, which
is the sanctioned route for a bake change, and the 52 coarse tiles are consumed
as-is (the bake runs CPU-only in ~2 min/tile on this box).

THE CHANGE (bake_ver 5). thermal.relax accepts a per-cell repose_deg field;
noise.repose_field builds it from two world-anchored components:
  * spatial lithology octaves (160 m and 40 m, +-6 deg RMS) -- patches of
    stronger and weaker rock, so adjacent noses of one ridge finish at
    different angles;
  * elevation-keyed strata bands (30 m and 7.5 m vertical wavelength, +-14 deg
    RMS) -- sub-horizontal bedding, so strong bands hold near-cliff faces and
    keep the relief incision carved into them while weak bands ravel back to
    talus: bench-and-cliff structure, which is exactly the "stop being a smooth
    ramp" escape named in the crookedness file, produced by process rather than
    painted on.
Clamped to [26, 60] deg. Both amplitudes at 0 skip the field and reproduce
bake_ver 4 bit-for-bit. Mass conservation and the steepest-pair stability bound
survive the field (donor-keyed threshold; tested, including numba-vs-numpy).

RESULTS -- filled in below as measured.

  slope distribution (same window; frac@repose = within +-7% of tan 36 deg):
    v4 B3:  p10/p50/p90 0.344/0.706/0.903   frac@repose 19.1%
    v5 B2d: p10/p50/p90 0.328/0.628/1.247   frac@repose 10.8%   (control: same
            statistics as v4's B2d, so the change is B3's and only B3's)
    v5 B3:  p10/p50/p90 0.345/0.641/0.979   frac@repose 14.9%

    The v4 histogram is discontinuous AT the repose value (4.85% density in the
    0.65-0.70 bin jumping to 9.08 / 14.14 / 10.17 in the three bins straddling
    tan 36); the v5 histogram is smooth through it (6.4 -> 7.4 -> 6.5 -> 5.6) and
    keeps a genuine steep tail to 1.3+ where v4 had cut it off. Thermal still
    moves the same amount of material overall (rms 1.39 m vs 1.33 m) -- talus
    still forms -- it just no longer planes every face to one angle.

  offline hillshade (D:\ue-cache\tmp\g35-v4-vs-v5-hillshade.png, v4 left):
    the uniform gray sheets between gullies become irregular facets, held rock
    pads and bench edges; the drainage layout is unchanged.

  vxc_terrainprobe, site -69120,38400, 512 m, worldgen v18 client (same probe
  binary, same window, boundary-cell counts quoted per the noise-vs-crookedness
  rule):

                        v4 (survey-g35)          v5 (survey-g35-v5)
    carrier D           1.006  (5080 bcells)     0.991  (4797 bcells)
    carrier ribs        p50 1.7 m  p90 9.7 m     p50 0.5 m  p90 5.0 m
                        p99 21.7 m max 26.5 m    p99 11.8 m max 12.7 m
    amplified D         1.020  (5287 bcells)     1.002  (4841 bcells)
    amplified ribs      p50 0.8 m  p90 2.3 m     p50 0.5 m  p90 1.6 m
    drainage carrier    0 sinks / 0.0% / 244 m   0 sinks / 0.0% / 243 m
    drainage amplified  0 sinks / 0.0% / 246 m   0 sinks / 0.0% / 245 m

    Rib length halves at every quantile WITHOUT boundary-cell inflation (the
    count FALLS), i.e. this is structure, not noise -- the failure mode the
    crookedness file warns about (D rising toward the 1.872 noise control on a
    10x boundary-cell jump) is absent. D itself barely moves, as expected:
    benches are locally straight too; what they change is spacing and length.
    Drainage is untouched at every stage. The known caveat stands: the bedding
    experiment improved rib p99 5x with no visual change, so the capture below
    is the deciding evidence, not this table.

  COST. Bake 298.7 CPU-s / 142.7 wall (B3.relax 208.5 CPU-s -- the field
  kernel roughly doubles the bake; it was already the stage a GPU eats).
  Peak working set 13.45 GiB on the first run; the strata pass is now chunked
  bit-identically and should return the pod budget to ~6 GiB (re-measure on
  the next full bake). Tile size unchanged (raw codec, fixed layout).

  in-engine raking capture at the repro site (owner's exact command: spawn
  -69094,38426, VistaShot=10, pitch -12, yaw 45, 200 s settle, both arms on
  the SAME main-checkout client build so the diff is bake-only):
    before: D:\ue-cache\tmp\repose-before.png  (provider survey-g35,
            peak loaded 54,616, fine tile resident, log capture-repose-before)
    after:  D:\ue-cache\tmp\repose-after.png   (provider survey-g35-v5,
            fine tile resident, log capture-repose-after)
    reading, honestly. The before frame is the artifact at its worst: long
    unbroken scallop rows wrapping the whole hillside at even spacing. The
    after frame breaks the single machined sheet into patches -- the mid-slope
    bowl loses coherent banding almost entirely and reads as talus; row
    lengths shorten and spacing varies -- but the big DISTANT faces still band
    legibly. The mechanism explains the residual: thermal transport is
    translation-invariant on a uniform ramp (every cell sheds exactly what it
    receives), so a strength field restructures the surface near THRESHOLD
    BOUNDARIES and leaves the interior of a uniform patch a uniform ramp --
    and a 60 deg ceiling is still a uniform ramp, just steeper. Iteration 2
    below raises the ceiling to a genuinely cliff-holding 72 deg, deepens the
    weak floor to 24 deg and the strata swing to +-20 deg.

  ITERATION 2 (v5b = the SHIPPED constants: spatial 8 deg / 160 m, strata
  20 deg / 30 m, clamp [24, 72]):
    slope p10/p50/p90/p99 over the 3.84 km window:
      v4  0.344/0.706/0.903/1.080   frac within +-7% of tan36: 19.1%
      v5  0.346/0.641/0.980/1.328                              14.9%
      v5b 0.347/0.628/1.000/1.452                              13.9%
    probe, same site/window: amplified D=1.011 (4824 bcells), ribs p50 0.5 m
    p90 1.9 m; carrier ribs p90 8.6 m p99 12.3 m max 14.3 m (v4: p90 9.7 p99
    21.7 max 26.5). Drainage: 0 sinks / 0.0% stranded / 243-245 m mean path
    at both stages -- byte-for-byte the v4 posture. Bake 307 CPU-s; peak WS
    11.0 GiB with the strata pass chunked (remaining excess over the 8 GiB
    pod note is the float64 spatial field + field-kernel buffers; chunk the
    spatial part the same way before any fleet bake).
    Capture: D:\ue-cache\tmp\repose-after2.png (provider survey-g35-v5b).
    Reading: the mid-slope bowl loses coherent banding and reads as talus
    with held rock pads; the upper rows become short ragged segments at
    variable spacing; the far-right face still carries a legible rhythm,
    interrupted. Against the before frame the "one machined topographic
    sheet" read is gone. Whether what remains passes the owner's eye is the
    owner's call -- the three frames are the evidence.

WHAT THIS DOES NOT FIX, AND THE RANKED REMAINDER. The field only acts where
thermal binds -- steep ground, which is the named blocker. Moderate sub-repose
faces (15-35% grade) keep whatever smoothness the carrier has there. In rank
order of expected value if more is needed:

  1. Strength-modulate INCISION with the same field (hard bands resist
     carving): knickpoints and bench treads at streams, extends the structure
     below the repose regime. Same bake-side machinery, one more consumer.
  2. Client meso band: restore 6.4 m / 12.8 m landform octaves on the fine
     tier under the existing v14/v15 cap. The cap prices gradient, and
     amplitude-per-gradient scales with wavelength, so ~0.5-1 m of relief at
     12.8 m costs what a sub-metre octave costs -- every previous additive
     attempt sat below 3.2 m where the cap forces sub-voxel amplitude. Needs
     the full worldgen/HLSL/version ceremony, hence ranked below the bake
     levers.
  3. Larger horizontal warp (the v16 mechanism at 30-60 m wavelength, metres
     of throw): bends and re-spaces contours structurally without touching
     drainage topology, but dilates every read window and the surface bound;
     and it cannot make benches, only wavier ramps.
