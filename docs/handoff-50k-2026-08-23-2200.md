# 50k/s + few-second cold start — state at 2026-08-23 22:00Z

## The goal (owner, restated many times)
50,000 chunks/s streaming; cold start "a few seconds" (currently ~41-46 s).

## THE BLOCKER, measured tonight
Game-thread ms per 5 s window, matched cold-start legs:

    ARM        recompute   dispatch   apply
    gp-ctl        246 ms     211 ms   692 ms
    pool-pri      266 ms   *1547 ms*   93 ms

Dispatch on the GPU-primary arm is **1,547 ms/window**, 20-40x the control, and
larger than recompute. Per forked chunk: 1547 / 10,391 `gpuForked` =
**0.149 ms of GAME THREAD per chunk routed to the GPU** = 7.4 s of game thread
per second at 50,000/s. This is the owner's standing priority task, unchanged.

NOT the raster atlas: it reports `fills=0, 0.0 ms GT, gpuMiss=0`. The 46 KB/chunk
sampling is already dead. Something ELSE per-chunk costs 0.149 ms.

## SECOND blocker, same arithmetic, nobody has looked at it
Apply is 0.054 ms/chunk on the control (692 ms / ~12,700 drains). At 50,000/s
that is **2.7 s of game thread per second**. Apply must also collapse.
Note every drained job is `zeroQuad` (marcher path, geometry lives in the brick
pool) -- so 0.054 ms is being spent on chunks that produce NO quads and NO
component. Nobody knows what it is spent on.

## THIRD, unexamined
`candidatesRejected` = 2.4M per 5 s window = **485,000 candidate evaluations/s**
to admit ~10k chunks. A 50:1 reject ratio inside recompute.

## Three counters that must be read TOGETHER
Any one alone points somewhere plausible and wrong. Burned twice tonight.
- `recompute=/dispatch=/apply=`  game-thread split
- `dispatched=/drained=`          pipeline balance (control 15611/15091 balanced;
                                  GPU-primary diverged to 7301/126 = 90k backlog)
- `exitCap=/exitEmpty=`           loop exit (`cap=288` = JobsInFlightPerCore 8 x 36)

**I nearly commissioned raising `cap=288` because `exitCap` dominated `passes`.
With a 90,000-deep backlog that would have made it strictly worse.** Check
`drained` before believing any cap reading.

## Landed tonight (all on main)
- `43aeaab` `-VoxelDispatchAheadCap` (default 4096 under `-VoxelGpuPrimary`):
  bounds dispatch-ahead-of-apply. Fixes R0-last ordering. NOT yet measured.
- `fd77973` six-bucket breakdown of `SubmitGpuMeshJob`'s game-thread cost
  (`VoxelWorldSubsystem.cpp:17797`). NOT yet read.
- Worklist stage 6 `-VoxelGpuWorklistClaim=1`: **per-chunk GPU submissions 5 -> 0**,
  spine constant 8 -> 11. Ledger 17->16->15->7->5->0. Flat in N. NOT yet measured.
- Three landed corruptions fixed in the pack->claim seam, all affecting arena
  slice > 0 only (mask base / desc rebase / allSolid source). Slice 0 was
  accidentally exact, which is why nothing caught them: the 1.95B-comparison
  gates verify the ARENAS; pool desc, record masks and allSolid were compared by
  nothing. **Do not compare any worklist arm against a pre-2026-08-23 capture.**

## Measurements so far
    gp-ctl     disp=12308  GPU= 3.7%  SETTLED 40.9s  R0 first  9.3s  evictions=0
    pool-pri   disp= 7843  GPU=66.3%  SETTLED 43.9s  R0 LAST  43.9s  evictions=0
    pool-wide  disp= 9899  GPU=69.8%  SETTLED 83.8s  R0 LAST  83.8s  evictions=0
    gp-ctl2    (rebuilt)              SETTLED 46.1s  R0 first  8.0s   <- control reproduces
    ahead-off  DIED AT STARTUP twice (22:11, 22:13), log ends mid-InternalLoadLibrary,
               no crash dump, no UE process left. gp-ctl2 works on the SAME binary,
               so it is the flags or the launch, not the build. No-dump + killed
               mid-load usually means killed, not faulted.

Cold-fill shape (pool-pri): peak **14,632/s** at t=3.3s decaying to ~700/s by
t=27s, with a dead window t=33.2-38.2s at `inFlight=0 pending=512` (~50-100/s)
that resolves itself. Peak rate held would be a ~11-13 s cold start.

## Lane rules
ONE editor/build lane. `af2d2da997b15b523` holds it. Every other agent:
DO NOT BUILD, DO NOT launch Unreal. Split agents by FILE, not phase --
`VoxelWorldSubsystem.cpp` (24k lines) is af2d2da's and is the serialisation point.
Read legs with `tools/leg-summary.sh`, never `grep | tail -1`.
