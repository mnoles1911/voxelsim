# Handover — shared progress checkpoint, 2026-09-07

> Latest steering (2026-09-07): the owner requested removal of all goblins,
> then rejected the wildlife visual quality and asked for a research-based
> redesign. Read [the new strategy](creature-reconstruction-strategy-2026-09-07.md)
> before further regeneration. Five goblins and their controls are removed;
> 382 wildlife remain. Creature (382 exports/12 rebuilds), publish fixtures,
> creation, LLM, pitch, frontend and quick selftest checks pass. Plant radius
> fixes are committed separately as 30398a2. Three approved crafts are published
> locally and byte/path verified. The real full publisher still reports stale
> environment exports and pending keep resync; the UE default root has not yet
> changed. Earlier completion counts below describe the previous checkpoint.


## User instructions and immediate status

The user asked to finish remaining planned work autonomously overnight, then
explicitly requested: **"commit and merge all progress made thus far. prepare
a handover doc for another agent session if necessary now"**.

This is a progress checkpoint, not a claim that the whole game or every plan
is complete. The checkpoint includes the shared workspace's existing source,
asset and design work as explicitly requested. Much of the UE/world work was
already present when this thread started and has not been independently
verified by this thread.

Working repository: `D:\voxelsim`. Source branch before checkpoint:
`lane/water-lighting-2026-09-06`, tip `1d1005a`, eight commits ahead of local
`main` (`b0114f2`). The requested merge target is local `main`. Remote is
`https://github.com/mnoles1911/voxelsim.git`; fetch succeeded. Check git history
for the actual checkpoint and merge IDs rather than assuming these old tips.

## Completed creature work

- Regenerated all **382 wildlife species** at seed 7. The existing saved
  raven was refreshed from its saved parameters; original copies are in the
  ignored `asset-forge/out/creature-refresh/originals/` directory.
- Added five goblins at seed 1: raider, spearhunter, stalker, hexer, brute.
  They have distinct gear, detailed faces and articulated part tags.
- 368 wildlife plus five goblins use **12.5 mm** voxels; 14 large wildlife
  species use coarser pitches under the measured generation budget.
- Curved-surface eyes, quadruped facial detail, tapered/curved coat stripes,
  folded-wing relief and fish mouth/gill detail are implemented.
- Fixed actual part ownership at three broken joint contacts: fennec fox,
  wood thrush and yellowhammer. These fixes leave geometry/materials intact.
- All 387 model entries have VOX/VXA/spec/realized/meta/thumbnail files under
  `asset-forge/library/`. Catalog regenerated.
- Goblins retain the existing quadruped engine kind with a Goblin mobs
  subgroup. This supplies model/rig data, not combat AI or animation clips.

Read `asset-forge/docs/creature-refresh-2026-09-06.md` and its JSON report,
plus `asset-forge/docs/goblin-mobs-2026-09-06.md`. The ignored review outputs
can be recreated by `tools/creature_review.py` and `tools/goblinprobe.py --write`.

## Verification actually performed

From `asset-forge`:

- `python tools/creatureprobe.py`: **PASS**, all 387 saved exports checked;
  17 exact deterministic rebuild comparisons; curved-surface regression.
- `python tools/goblinprobe.py --write`: **PASS**, five models, complete
  face connectivity, joint contacts and exact 12.5 mm VXA round trips.
- Targeted repaired rigs: seeds 1, 7 and 19 passed for all three species.
- `python tools/export_categories.py --check`: **PASS**, 845 total species,
  including 387 creatures.
- Frontend TypeScript/Vite build: **PASS** after goblin UI integration.
- Live HTTP API: verified goblin controls and 387 creature library records.
- `python tools/publishprobe.py`: **PASS** after the checkpoint's new craft
  publishing implementation, including draft/reject/approve transitions,
  stale-file/index detection, corrupt-source preservation, exact pitch,
  deterministic output and read-only resync reporting.
- `python -m forge.cli selftest --quick`: **FAIL**, four existing environment
  warnings: black-coral-tree, bramble-thicket, carnation-soft-coral and
  cold-water-coral have tip radii below half their authored voxel pitch.
  Its full-library environment generation checks were deliberately skipped.
- No full UE build or in-engine runtime verification was performed for this
  shared-workspace checkpoint. Do not label the merged UE code fully verified.

## Overnight work started, unfinished

Use `docs/asset-forge-overnight-work-2026-09-06.md` as the task ledger and
`docs/asset-forge-app-plan-2026-09-05.md` for acceptance gates. The latter has
stale opening claims; its later owner rulings and actual code take precedence.

Implemented during the overnight start:

1. `tools/publish.py --check-only` now calls resync with `dry_run=True`.
   Previously even this check command rewrote curation. Pending changes or
   rejected-with-kept conflicts now produce a failing status.
2. New `forge/publishing.py` exports only approved saved craft into an isolated
   staged directory with its own relative-path `categories.json`, validates
   VXA/pitch/materials and replaces the live craft directory with rollback.
   Windows can transiently deny a renamed-directory replacement; bounded
   retries were added and the regression probe passes.
3. The publish CLI calls this craft exporter/checker. Unit-style negative
   gates live in `tools/publishprobe.py` and use isolated fixture directories.

**Still to do:**

- Publish the real approved craft set and verify copied paths/bytes. Current
  approved saved crafts are canoe, glider and bushcraft-bamboo-raft. Other
  craft drafts must remain omitted; do not invent human approval.
- Change `VoxelAssetBody.cpp::DefaultLibraryRoot()` from raw
  `asset-forge/library` to the verified `asset-forge/out/engine/craft` path
  only after the real published directory is populated. This change has
  **not** been made. The cvar override remains the dev escape hatch.
- Validate the full publisher against real exports. The new craft path has
  passed fixtures, not a full production publish. Full publish resyncs kept
  seeds into curation and may change `species.vxm`; honor the existing
  worldgen-version obligation. Do not casually re-bless unrelated goldens.
- Resolve the four plant warnings without weakening the warning. Run the
  relevant generator checks, quick selftest and planned remaining gates.
- Verify current creation/Claude/pitch probes and finish any documented gaps.
- Reconcile any still-open rigid-part animation requirements with code before
  implementation; do not invent new gameplay design from a broad instruction.
- Complete the appropriate UE build/capture gates for the craft root change.

## Shared work included but not independently certified here

Existing changes cover binary voxel pitch support; detached-object ownership,
networking and persistence; async saves; environment slope fitting and sparse
grids; vegetation materials/wind/capture; mining/crafting UI; stone-axe/tree
felling prototypes; and the crafting progression/design graph and handbook.
The corresponding dated docs, `tools/verify-*` scripts, UE tests and voxel-core
tests are included. Read each lane's evidence rather than inferring readiness
from inclusion in this checkpoint.

## Runtime and continuation

Asset Forge was restarted from this checkout at `http://127.0.0.1:8731` using
Python `-m forge.cli serve --port 8731 --no-open` (PID was 11380; recheck).
It must be restarted again to load subsequent Python changes. Its log files
are in ignored `asset-forge/out/creature-refresh/`.

An hourly thread heartbeat was created for 12 runs:
`finish-asset-forge-planned-work-overnight`. It continues the scoped task from
the ledger. Coordinate or pause it before another session edits the same files;
disable it when the work is complete. No other working subagent remains active.

Use `tools/voxel-build.ps1 -Verify` for UE builds. Its busy-process guard is
intentional; do not start a competing editor/build or bypass a live collision.
Avoid assuming an exit code alone proves UE build success: the script checks
the build result and second-build coherence.

Downloaded `.scratch/raft-python/` dependencies and root `lumen.pdf`/`lumen.txt`
research inputs stay local and ignored. Source scratch probes are preserved.
No files were deleted to hide unfinished work or obtain a clean checkpoint.
