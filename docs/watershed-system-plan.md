# MOVED — see `water-system-architecture.md`

This file's content was not superseded; it was **renamed**. It is now
`docs/water-system-architecture.md`, which is to water what
`world-generation-architecture.md` is to land.

The rename happened because the name was wrong twice over. "Plan" implied
something forward-looking and disposable, and the file had already stopped being
one — it opened by saying it *replaced* the plan that used to live at this path.
And "watershed-system" named a subsystem when the file actually describes every
stage from rainfall to pixels.

The new file additionally absorbs `water-deep-dive-brief-2026-08-05.md`, so
there is now one durable document rather than a how-it-works and a what-is-wrong
that had to be read together and had begun to disagree.

Nothing in this file is preserved here. Use git history if you want the text
under its old name.
