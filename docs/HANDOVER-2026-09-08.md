# Handover, 2026-09-08: loading gate clock, water look, music pools, journal/resize, wake + dry cockpit

Continues `docs/HANDOVER-2026-09-07-evening.md`. Everything below is committed unless marked.
The box was shared with a Codex session all night (up to 17 editor commandlets), so every
timing number from these launches is VOID; verdicts on control flow and images stand.

## Commits, in order

| hash | set | state |
|---|---|---|
| `943686c` | `docs/music-design.md` (accepted pool architecture), `docs/music-generation-prompts-2026-09-08.md` (22 cues, 14 new Suno prompts + 8 catalogue), backlog §15c stingers, "title screen passes" row | owner to generate the cues in Suno |
| `d612e96` | Loading gate: `MaxWaitSeconds` judged on the WALL clock (`FVoxelWorldReadyProbe`); the live "looping loading screen" root cause | verified live: `READY after 17.74s wall` on the next NEW GAME |
| `a0cc5f6` | Water: shallow turbidity floor, submerged bed darkening, per-channel extinction; five regenerated materials | owner: "good enough for now" |
| `8e1e3d8` | Music pools: priority ladder, authored 45-120 s silence, four time-of-day slots, 3 s crossfade, per-pool memory, folder banks; 29 WAVs moved into pool folders | built green, never run live; selection log line is the check |
| `ecec74e` | Journal/codex drew at 62%/58% under the shell ScaleBox (fixed by bounding columns); drag-resize grip on the unified shell | built green, needs an owner capture |
| `4c5c683` | Wake defaults (speed 0.5, half-life 8, gain 1.5, width 0.3) + hull ripple ellipse mask through `MPC_VoxelSky`; full sky-chain regen; chain tool classification; three camera-cache guards that let unattended legs survive frame 1 | headless boat leg passed: `hull ripple mask ENGAGED halfLen=187 halfBeam=45`, no default-material fallback, frame `VoxelVerify00992.png` (cam -35 pitch, +12 m, boat under way); owner judges the image |

| `ee1647f` | UI backlog (dead title constants, journal seed anchored to today, HUD hidden under overlays at z 109, death stamp with season/year/era) + one reader for the ring-radius switch | compiled in a full-tree pass; Verify coherence unprovable while Codex edited concurrently; death card and journal date line grew and want an owner capture |

| `235be20` | Shell chrome trim for Menu Size >= 1.40 / INTERFACE SIZE 1.50 (1080p itself needed nothing: the shell sees 1080 units at every resolution); UI capture tool -ResX/-ResY made real | built green, coherence proved; 1080p before/after pairs in docs/ui-mocks/2026-09-08/1080p; owner judges the Menu Size 1.50 proportions |

| `39eba79`, next | Quiet-box loading: lake tier loads tiles on a worker (13.6 s freeze gone); dispatch loop wall-clock budget under the theatre (p99 under the curtain 9 s -> 21 ms); trace scopes + hitch line fix; loader switches on for interactive launches; 16 headless tests pass (2 GpuPool fails are Codex's uncommitted record growth) | one 5.3 s first-recompute freeze remains at spawn |

## Owner verdicts this session

Accepted: title screen; map (marker, zoom, pan, marks); hold-Tab cursor; water look over the
snow shore. Open, with work landed: wake "much better but still much too large, spread too far,
last too long" (defaults changed, not yet seen); "water and wake, surface effects inside the
canoe" (hull ripple mask, not yet seen); journal too small (fixed, not yet seen); panel
drag-resize (added, not yet seen).

## Pending / next session, cheapest first

1. **Owner launch** with the standard line and judge: wake size/spread/persistence, dry cockpit
   (log must show `VoxelBoat: hull ripple mask ENGAGED` and no `MPC_VoxelSky has no HullEllipseA`
   warning), journal at 100%, the resize grip (bottom-right corner, cursor changes), music
   selection lines `VoxelUIMusic: pool= slot= cue= gap=` and the 3 s crossfade at the reveal.
2. **Music content**: generate the 22 cues; drop them into the pool folders (filenames become
   the now-playing label). Night is one cue plus the Day floor until then. Re-encode the
   Lirien-Thal MP4 (command in `MUSIC_CREDITS.md`). Stub signals to wire when they exist:
   Combat, Town, Rain, Stingers (`VoxelUIMusic.cpp`, one predicate each).
3. **Loading-screen smoothness**: DONE to p99 21 ms under the curtain (see the tile-loading doc,
   "Quiet-box loading measurement"). Left: the first ring recompute's admission loop, 1.7 + 5.4 s
   once per spawn -- split the per-level admission across ticks (the Recompute scopes and
   `Saved/loading-leg7.utrace` show exactly where).
4. **Loader default flip**: DONE for interactive launches in `tools/voxel-editor.ps1`; headless
   legs keep 0 on purpose.
5. The pause footer still says "Day N" on purpose (a judged screen); widen it only on an owner verdict. The death stamp prints "of the Second Age" from the mock; one line in WorldStamp removes it if the fiction goes.
6. Map and player screens under the same ScaleBox: measured 2026-09-09 at 1440p, body 1366 px on
   both, factor 1.0 -- nothing shrinks; closed without a code change.
7. Backlog carried: menu backdrops at 3840 (needs Mira-Thal originals), Press Start 2P font;
   shell width is not trimmed (only 4:3 at INTERFACE SIZE 1.50 would need it).

## Rules this session paid for

- Every timeout that guards a curtain runs on the wall clock. The world clamps DeltaSeconds
  to 0.4 s; three clamped frames hid nine minutes.
- A ScaleBox scales by DESIRED size; an auto-wrapping text block with no width asks for its
  longest line. Bound fluid columns; the ScaleBox is for rigidity only.
- Any helper graph that binds `MPC_VoxelSky` must be classified in
  `tools/voxel-sky-chain-regen.ps1` or the chain refuses; a full chain (not `-Only`) after any
  MPC parameter change, then a capture, because a broken dependent compiles to the default
  material while every log line says ok.
- A PowerShell `Start-Job` dies with the tool call; background waits go through Bash.
- When another session has an uncommitted working set in the same module, stage by HUNK
  (`git diff` filtered by keyword, `git apply --cached --ignore-whitespace`), never by file,
  and expect Verify coherence to be unprovable while they type.
