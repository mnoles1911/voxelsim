# Front-end switch classification -- the input to
# tools/lint-frontend-switch-coverage.py.
#
# WHAT THIS FILE IS FOR
# ---------------------
# VoxelFrontEndPolicy decides, per run, whether the main menu appears. Its
# rule 5 suppresses the menu whenever a "self-driving" switch is on the command
# line -- one whose run spawns, poses, waits and captures with no human
# present. Such a run must never stop at a menu nobody is there to click.
#
# That classification is a NAMING-CONVENTION RULE in VoxelFrontEndPolicy.cpp
# (a switch containing Test / Shot / After / Check / Verify / ... drives
# itself), not a list, because a list of ~190 names is wrong the first week
# somebody adds -VoxelFooTest and forgets to update it.
#
# The rule cannot classify everything, though, and the failure is silent: a new
# -VoxelSomethingFixture would fall through as "tuning", the menu would appear,
# and the capture would hang until the watchdog killed it. This file is what
# makes that failure LOUD instead. Every switch the rule does not match must
# appear below, confirming a human decided it is ordinary tuning or
# configuration. The lint fails on anything unlisted.
#
# TO ADD A SWITCH
# ---------------
#   * Does the run drive itself and quit?      Name it *Test / *Shot / *After,
#                                              and the rule handles it.
#   * Is it configuration or tuning?           Add its name here.
#   * Neither of those fits?                   Add it to kSelfDrivingExtras in
#                                              VoxelFrontEndPolicy.cpp, with a
#                                              reason.
#
# Blank lines and #-comments are ignored. One switch name per line, without the
# leading dash and without a trailing '='.

VoxelAdmissionBandSkip
VoxelAssetDir
VoxelBuriedSkip
VoxelCameraHigh
VoxelCaveEV
VoxelCaveLampLumens
VoxelCaveLampRadiusM
VoxelCaveLight
VoxelCavePitch
VoxelCaveSettle
VoxelCavernFlooded
VoxelCoarseGrid
VoxelCoarseMinLevel
VoxelDate
VoxelDebugRings
VoxelDefaultMaterial
VoxelDetailRingMeters
VoxelDetailShadows
VoxelDigDownDepth
VoxelDigDownQuit
VoxelDigDownSettle
VoxelFineTileCacheBudgetGB
VoxelFineTileDir
VoxelFineTileGateFatal
VoxelFineTileProviderId
VoxelFineTileRingRadius
VoxelFlySpeedStep
VoxelForceUnderwaterSpawn
VoxelGIBreach
VoxelGICavePitch
VoxelGICaveSettle
VoxelGICaveStandoff
VoxelGICaveTorch
VoxelGICaveTorchSettle
VoxelGIDebug
VoxelGILegacyBasis
VoxelGILegacyRefresh
VoxelGIOn
VoxelGIQuadSpan
VoxelGIVis
VoxelGpuJobLean
VoxelGpuMeshMaxLevel
VoxelGpuWorklist
VoxelGpuWorklistAssetStamp
VoxelGpuWorklistBudget
VoxelGpuWorklistCap
VoxelGpuWorklistCellBudget
VoxelGpuWorklistClaim
VoxelGpuWorklistClassify
VoxelGpuWorklistColumns
VoxelGpuWorklistPack
VoxelGpuWorklistVoxelize
VoxelL0BrickSkip
VoxelL0GridScratch
VoxelLakeSheetBands
VoxelLakeSheetCells
VoxelLakeSheetFineM
VoxelLakeSheetRangeM
VoxelLakeSheetSnapPx
VoxelLakeSheets
VoxelMaxRingLevel
VoxelMipCacheBudgetMB
VoxelNoClipmap
VoxelNoColdBandThrottle
VoxelNoDetailAssets
VoxelNoEditFloorHatch
VoxelNoLoad
VoxelNoPSOPrecache
VoxelNoRingSkirt
VoxelNoUnderground
VoxelOceanDigDepthM
VoxelOceanDigHeadingDeg
VoxelOceanDigLenM
VoxelOceanDigRadiusM
VoxelOceanDigReport
VoxelOverlayOn
VoxelOverlayRow
VoxelPendingJobCap
VoxelPerfDepth
VoxelPerfExitWatchdogSec
VoxelPerfHeading
VoxelPerfLingerSec
VoxelPerfLogInterval
VoxelPerfPitch
VoxelPerfPreflightSec
VoxelPerfSpeed
VoxelPerfYaw
VoxelPoolCapacityQuads
VoxelRingCrossFade
VoxelRingFloors
VoxelRingInnerMeters
VoxelRingOuterMeters
VoxelRingQuota
VoxelRiverRibbonMinPx
VoxelRiverRibbonRangeM
VoxelRiverRibbons
VoxelSeed
VoxelShadowCascades
VoxelSkySkip
VoxelSkyTrim
VoxelSpawnAltM
VoxelSpawnAt
VoxelSpawnPitch
VoxelSpawnYaw
VoxelStaticRelevance
VoxelSweBreachAt
VoxelSweBreachFillPct
VoxelSweBreachSampleSec
VoxelSweBreachSearchM
VoxelSweBreachSwe
VoxelTileDir
VoxelTileScale
VoxelTimeOfDay
VoxelTimeScale
VoxelUndergroundDepth
VoxelUndergroundVeil
VoxelVeilExtentM
VoxelVistaPitch
VoxelVistaYaw
VoxelWaterHover
VoxelWaterLateralFillPx
VoxelWaterMarkerFillPx
VoxelWaterUnderside
VoxelWindFromDeg
VoxelWindMps
VoxelZstdDll

# --- 2026-08-22 batch: streaming/pool/perf work ------------------------------
# All tuning or configuration; none drives the run. The perf DRIVER switches
# (-VoxelPerfRun, -VoxelPerfStaticAt) are already in kSelfDrivingExtras;
# VoxelPerfHeightM is a pose parameter alongside the existing VoxelPerfDepth.
# VoxelNoGpuMesh left this list the same day: the fork's default flipped to
# OFF and the switch became VoxelGpuMesh (the enable arm).
VoxelAsyncAssetResolve
VoxelAsyncAssetResolveWarm
VoxelNoIncrementalAdmission
VoxelNoBucketedExitScan
VoxelApplyBudgetMs
VoxelApplyDrainCap
VoxelApplyPerTick
VoxelBandSeedCpu
VoxelBrickPack
VoxelBrickPackOnCpu
VoxelBrickPoolChunks
VoxelClipmapSquareHole
VoxelColdBandDeferPark
VoxelDispatchAheadCap
VoxelGIOff
VoxelGpuBrickFlushBatch
VoxelGpuMesh
VoxelGpuMeshQueueDepth
VoxelGpuPoolAlloc
VoxelGpuWorldGenBatch
VoxelEditedLaneGate
VoxelRadialCensus
VoxelRecomputeCensus
VoxelResidencyAdmitBudget
VoxelRingOrderScan
VoxelHierarchicalCoverage
VoxelInnerHysteresisChunks
VoxelNoHierarchicalCoverage
VoxelNoSharedGridCache
VoxelPerfHeightM
VoxelRetireQuads
VoxelRingFloorCpuOnly
VoxelRingOverlapChunks
VoxelRingSkirtPaddedInner
VoxelShadowDistanceM
VoxelSharedGridCache

# --- The front end's own switches -------------------------------------------
# Configuration and tuning for the menu itself. None of these drives the run;
# the ones that DO drive it (-VoxelMenuShot, -VoxelLoadingShot,
# -VoxelHourglassShot, -VoxelMenuAutoStart) are named in
# kFrontEndCaptureSwitches in VoxelFrontEndPolicy.cpp instead, where they force
# the front end ON rather than off.
VoxelFrontEnd
VoxelNoMenu
VoxelMenuPanel
VoxelMenuAutoStart
VoxelMenuWatchdog
VoxelUINoAssets
VoxelLoadGateMaxRing
VoxelLoadMinHold
VoxelLoadMaxHold

# --- 2026-08-23: far-clipmap appearance (docs/backlog.md 0.0d) ---------------
# All tuning, diagnosis or configuration; none of them drives the run, so all of
# them belong here rather than in kSelfDrivingExtras.
#
# VoxelClimateSentinelGuard=0 turns OFF the floor that stops terrain writing the
# byte pattern M_VoxelTerrain/M_VoxelClipmap reserve for a debug marker. It is
# the A/B arm for the magenta fix, not a feature switch -- passed, cold-and-dry
# ground draws bright magenta again on purpose. See VoxelClimateProbe.h's
# kSentinelSafeFloorU8 for the measurement.
#
# The four VoxelClipmapSnow*/Snowline* switches move M_VoxelClipmap's snow
# parameters at runtime through a per-level MID. With none of them passed no MID
# is created and the frame is byte-identical to one taken before the change,
# which is why they are tuning rather than a mode.
#
# VoxelClipmapColorCensus only adds log lines. It is deliberately NOT named
# *Check or *Verify: those endings make VoxelFrontEndPolicy treat the whole run
# as self-driving and suppress the menu, and this switch is meant to be readable
# on an ordinary interactive flight where the owner is the one looking at the
# vista.
VoxelClimateSentinelGuard
VoxelClipmapSnowlineLowM
VoxelClipmapSnowlineHighM
VoxelClipmapSnowTempMax
VoxelClipmapSnowTempFeather
VoxelClipmapColorCensus
#
# The cold-start settle probe's four switches only tune an always-on log
# instrument (hold/timeout/cadence) or turn it off; none of them makes a run
# drive itself, and the probe is meant to be readable on ordinary interactive
# launches -- which is exactly why the names avoid *Check/*Verify endings.
VoxelNoColdSettle
VoxelSettleHoldSec
VoxelSettleTimeoutSec
VoxelSettleProgressSec
#
# -VoxelRecomputeDutyPct caps the share of wall time the anchor recompute may
# occupy (see VoxelStreamAdmission::RecomputeDutyPct). It is a streaming tuning
# knob: 0 is the control arm, it drives nothing and quits nothing, and it is
# meant to be usable on an ordinary interactive flight -- which is exactly why
# the name avoids a *Check/*Verify ending that would suppress the menu.
VoxelRecomputeDutyPct

# --- 2026-08-23: the apply path's per-chunk worldgen sampling ----------------
# ue-project/Source/VoxelEarth/VoxelApplyBatch.h. All three are tuning: they
# change how DrainResults computes a brick chunk's shading, and none of them
# drives, poses or ends a run. VoxelApplyColumnCacheAudit is deliberately named
# *Audit and NOT *Verify -- the naming rule in VoxelFrontEndPolicy.cpp treats a
# switch containing "Verify" as self-driving and would suppress the front end
# for what is a correctness cross-check on an ordinary interactive run.
VoxelApplyFast
VoxelApplyColumnCache
VoxelApplyColumnCacheAudit

# --- 2026-08-23 late: the drain loop's per-tick ceilings ---------------------
# ue-project/Source/VoxelEarth/VoxelApplyBatch.h. Latched overrides for
# voxel.Stream.MaxAppliesPerFrame, voxel.Stream.ApplyBudgetMs and the
# kMaxResultDrainsPerFrame constexpr, because -ExecCmds lands after streaming
# has begun and a cold-fill leg driven that way measures a blend of two caps.
# Tuning: they change how much the drain loop may do per tick, and none of them
# drives, poses or ends a run.
VoxelApplyCap
VoxelApplyBudgetMs
VoxelApplyDrainCap
# --- 2026-08-23: the fine-tier sampler lock ---------------------------------
# ue-project/Source/VoxelEarth/VoxelFineLockMeter.h and VoxelFineTileStreamer.
# Both are tuning/instrument: -VoxelFineLockFast picks how RequestFootprint
# acquires the streamer's RWLock (0 = today's unconditional exclusive), and
# -VoxelFineLockMeter arms the acquisition counters. Neither drives, poses or
# ends a run, and both are meant to be readable on an ordinary interactive
# launch.
#
# THE METER IS NOT CALLED *Probe ON PURPOSE, and the near-miss is worth
# recording: "Probe" is in kSelfDrivingSubstrings, so -VoxelFineLockProbe would
# have matched the rule, suppressed the main menu, and never appeared in this
# file at all -- a passive counter quietly taking the front end away from a
# developer who only wanted the numbers. Renamed rather than excepted, because
# the rule is right and the name was wrong.
VoxelFineLockFast
VoxelFineLockMeter

# --- 2026-08-23 late: frame-phase reconciliation ----------------------------
# ue-project/Source/VoxelEarth/VoxelFramePhase.h. A passive per-frame meter
# that splits a frame into voxelTick / gameOther / gameWait / RESIDUAL and
# prints the residual explicitly, so "not the render thread" is a sayable
# result. -VoxelFramePhaseHeavy= sizes the heavy-frame bucket. Instrument and
# tuning; neither drives, poses or ends a run, and both are meant to be
# readable on an ordinary launch.
#
# NOT named *Probe or *Verify, for the reason VoxelFineLockMeter records: those
# substrings are in kSelfDrivingSubstrings and would silently take the front
# end away from a developer who only wanted the numbers.
VoxelFramePhase
VoxelFramePhaseHeavy
# The moving/parked split and the two-part Goal 3 gate (owner, 2026-08-23:
# "steady and above 100 when moving at 20 meters per second at least").
# MoveUU borrows admission's own 100 UU/s constant; GateMps is the 20 m/s
# floor; SteadyPct is the operationalisation of "hitches ~ 0" and is a switch
# precisely because it is a judgement call.
VoxelFramePhaseMoveUU
VoxelFramePhaseGateMps
VoxelFramePhaseSteadyPct
VoxelAdmissionCapDrainSec
VoxelAdmissionCapMax
VoxelBrickPoolMatMiB
VoxelBrickPoolOccMiB
VoxelBucketedExitScan
VoxelCutoffClamp
VoxelCutoffHeadroomDiv
VoxelGpuBandColdOnly
VoxelGpuBandSeedOnly
VoxelGpuLeanBrickJobs
VoxelGpuMeshBatchCap
VoxelGpuMeshHarvestCap
VoxelGpuMeshSpecCap
VoxelGpuPrimary
VoxelGpuRasterAtlas
VoxelGpuRasterAtlasClimateAudit
VoxelGpuRasterAtlasClimatePitchMm
VoxelGpuRasterAtlasFill
VoxelGpuRasterAtlasFillBatch
VoxelGpuRasterAtlasFillMs
VoxelGpuRasterAtlasFillTasks
VoxelGpuResidency
VoxelGpuStackClaim
VoxelGpuWorklistBandChunks
VoxelIncrementalAdmission
VoxelLeadHorizonSec
VoxelNearestAdmit
VoxelPerRingRelax
VoxelRingScale
VoxelSpecLeadMaxM
VoxelSurfaceMip
VoxelUnloadBandChunks
VoxelUnloadRingMult
VoxelVelocityBias
VoxelViewBias
VoxelWorkerPool
VoxelWorkerTaskPri

# ue-project/Source/VoxelEarthShaders/Public/VoxelRenderFrame.h. The
# render-frame split: it names where the render thread's 9.23 ms parked /
# 18.60 ms moving actually goes (scene-renderer setup, RDG execute, and
# everything else), reconciled against GRenderThreadTime with the delta
# printed. Passive per-frame meter, off by default, no run behaviour.
# -VoxelRenderFrameMutate= / -VoxelRenderFrameMutateMs= are the negative
# controls: they inject a known cost or a known BLOCK into a named bucket so
# the split can be observed FAILING rather than only observed passing. All
# three are configuration; none of them drives a run or quits it.
VoxelRenderFrame
VoxelRenderFrameMutate
VoxelRenderFrameMutateMs

## SUBSTRING-MATCHED -- ACKNOWLEDGED
#
# Everything below is a switch whose NAME contains one of rule 5's substrings,
# so VoxelFrontEndPolicy already treats it as self-driving and suppresses the
# main menu whenever it is on the command line. Until 2026-08-23 the lint
# SKIPPED every one of these ("the rule handles it") -- which meant a switch
# that matched BY ACCIDENT was both changing front-end behaviour and invisible
# to the one lint built to catch that. -VoxelFineLockProbe was caught by a
# human, by luck.
#
# The suffix advice the lint prints does not fix the class either:
# -VoxelGpuMeshInFlight ENDS in "Flight" and is a job-count cap. Intent cannot
# be read off a name, so the decision is RECORDED here rather than inferred.
#
# Format: Name = reason. The lint requires a reason of real length -- the rule
# kSelfDrivingExtras already uses -- because an unexplained entry is how a
# table starts rotting.
#
# 13 entries are marked ACCIDENTAL. None is renamed here: renaming mid-campaign
# would break leg commands, tools and docs other agents are running against
# right now. Each accidental entry names the rename it wants and the owner.
VoxelAutoDigAfter = Timed action inside a self-driving run: it fires at an offset and the run ends itself.
VoxelBreachTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelBucketedExitScanVerify = ACCIDENTAL ('Verify'). Correctness arm for the bucketed exit scan in the eviction index; verifies during a normal run. Owner: VoxelEvictionIndex.*
VoxelCaveTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelCavernShot = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelChopTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelClientScreenshotAfter = Timed action inside a self-driving run: it fires at an offset and the run ends itself.
VoxelCoarseGridVerify = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelCollapseTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelDigDownTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelDigSwarmTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelDumpAgentsAfter = Timed action inside a self-driving run: it fires at an offset and the run ends itself.
VoxelDumpDigestAfter = Timed action inside a self-driving run: it fires at an offset and the run ends itself.
VoxelExecAfter = Timed action inside a self-driving run: it fires at an offset and the run ends itself.
VoxelFloodTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelGICaveTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelGIColorCheck = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelGIConverge = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelGIConvergeLegacy = Parameter of a self-driving fixture; only meaningful when its fixture switch is also present, so suppressing the menu is correct.
VoxelGIConvergeSeed = Parameter of a self-driving fixture; only meaningful when its fixture switch is also present, so suppressing the menu is correct.
VoxelGIConvergeSettle = Parameter of a self-driving fixture; only meaningful when its fixture switch is also present, so suppressing the menu is correct.
VoxelGIRelightAfter = Timed action inside a self-driving run: it fires at an offset and the run ends itself.
VoxelGITest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelGpuMeshInFlight = ACCIDENTAL ('Flight', and as a SUFFIX). A job in-flight cap, pure tuning. THIS IS THE ENTRY THAT PROVES A SUFFIX RENAME CANNOT FIX THIS CLASS: it already ends in the substring the lint tells you to rename to. RECOMMEND RENAME to -VoxelGpuMeshJobCap by whoever owns VoxelWorldSubsystem.cpp.
VoxelGpuWorklistVerifyCT = ACCIDENTAL ('Verify'). Byte gate for the worklist ClassifyTotals stage, not a fixture: it adds a compare pass and ends nothing. Not renamed, because every leg command, tools/leg-summary.sh and docs/gpu-worklist-plan-2026-08-23.md cite it mid-campaign; suppressing the menu is harmless on the headless legs it is actually used on.
VoxelGpuWorklistVerifyClaim = ACCIDENTAL ('Verify'). Byte gate for the worklist Claim stage. Same reasoning as VerifyCT.
VoxelGpuWorklistVerifyCols = ACCIDENTAL ('Verify'). Byte gate for the worklist Column stage. Same reasoning as VerifyCT.
VoxelGpuWorklistVerifyPack = ACCIDENTAL ('Verify'). Byte gate for the worklist Pack stage. Same reasoning as VerifyCT.
VoxelGpuWorklistVerifyStamp = ACCIDENTAL ('Verify'). Byte gate for the worklist AssetStamp stage. Same reasoning as VerifyCT.
VoxelGpuWorklistVerifyVox = ACCIDENTAL ('Verify'). Byte gate for the worklist Voxelize stage. Same reasoning as VerifyCT.
VoxelHeadlessDigTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelHourglassShot = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelHudShotOnly = Parameter of a self-driving fixture; only meaningful when its fixture switch is also present, so suppressing the menu is correct.
VoxelJobsInFlightPerCore = ACCIDENTAL ('Flight'). Per-core job cap -- the cap=288 knob the 50k budget doc reads. Pure tuning. RECOMMEND RENAME to -VoxelJobsPerCoreCap by whoever owns VoxelDebug.cpp.
VoxelL0GridCacheProbe = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelL0GridVerify = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelLoadingShot = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelMatHistogram = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelMenuShot = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelOceanDigAfter = Timed action inside a self-driving run: it fires at an offset and the run ends itself.
VoxelOceanSurvey = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelOceanSurveyStepM = Parameter of a self-driving fixture; only meaningful when its fixture switch is also present, so suppressing the menu is correct.
VoxelOverlayShot = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelPerfFlight = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelReadyProbeLog = ACCIDENTAL ('Probe'). Enables a readiness log line; a diagnostic, not a run that drives itself.
VoxelReplicationSelfTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelSaveWorldAfter = Timed action inside a self-driving run: it fires at an offset and the run ends itself.
VoxelScreenshotAfter = Timed action inside a self-driving run: it fires at an offset and the run ends itself.
VoxelScreenshotBurst = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelScreenshotBurstIntervalSec = Parameter of a self-driving fixture; only meaningful when its fixture switch is also present, so suppressing the menu is correct.
VoxelSkyLadder = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelSkyLadderAltA = Parameter of a self-driving fixture; only meaningful when its fixture switch is also present, so suppressing the menu is correct.
VoxelSkyLadderAltB = Parameter of a self-driving fixture; only meaningful when its fixture switch is also present, so suppressing the menu is correct.
VoxelSkyLadderAltCvar = Parameter of a self-driving fixture; only meaningful when its fixture switch is also present, so suppressing the menu is correct.
VoxelSkyLadderPreflight = Parameter of a self-driving fixture; only meaningful when its fixture switch is also present, so suppressing the menu is correct.
VoxelSkyLadderSettle = Parameter of a self-driving fixture; only meaningful when its fixture switch is also present, so suppressing the menu is correct.
VoxelSkyLadderStartHour = Parameter of a self-driving fixture; only meaningful when its fixture switch is also present, so suppressing the menu is correct.
VoxelSpawnWaterTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelStructureTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelSwarmTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelSweBreachTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelTier1RegionGraphTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelTreeTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelUndergroundTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelVerifyBuriedSkip = ACCIDENTAL ('Verify'). Runtime correctness arm for the buried-chunk skip: it verifies DURING a normal run and never drives or ends one. Arming it interactively would silently lose the main menu.
VoxelVerifySkyBand = ACCIDENTAL ('Verify'). Runtime correctness arm for the sky band; verifies during a normal run.
VoxelVerifySolidSkip = ACCIDENTAL ('Verify'). Runtime correctness arm for the solid-skip admission gate; verifies during a normal run.
VoxelVistaShot = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelWalkModeAfter = Timed action inside a self-driving run: it fires at an offset and the run ends itself.
VoxelWalkTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelWaterLoadCheck = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelWaterMemoTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelWaterParityTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelWaterPersistTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelWaterWakeTest = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
VoxelWindingCheck = Fixture: drives the run and exits on its own; the naming convention is doing exactly what it was designed to do.
