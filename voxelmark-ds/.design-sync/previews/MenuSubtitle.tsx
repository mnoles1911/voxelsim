import { MenuTitle, MenuSubtitle } from '@voxelmark/design-system';

export const UnderTheTitle = () => (
  <div style={{ background: '#0a0a0f', padding: '24px 0' }}>
    <MenuTitle />
    <MenuSubtitle />
  </div>
);
